# Refuse to install under a Node version the project does not support, instead
# of installing successfully and failing later with a confusing error.
# Applies to any package.json in this tree, so the frontend inherits it.
engine-strict=true
