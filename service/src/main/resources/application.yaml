server:
  port: 8080
signaldesk:
  fixture-dir: ../data/fixture
  sweep:
    cron: "0 */15 * * * *"      # the sense step: fires with no prompt
    enabled: true
  window-days: 7
spring:
  main:
    banner-mode: off
logging:
  level:
    com.signaldesk: INFO
