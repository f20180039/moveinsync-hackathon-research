reference/
.DS_Store
node_modules/
.next/
coverage/
target/
service/.venv/
__pycache__/
*.pyc

# MCP config — may carry API keys in env blocks. Never commit.
.mcp.json
.env
.env.local

# Agent scratch workspace (SDD ledger, briefs, review packages). Never content.
.superpowers/

# The provided dataset (~555MB). Never committed; pulled via scripts/fetch-dataset.sh
data/real/

# Claude Code agent worktrees — full repo copies, never content.
.claude/
# Task 17 synthetic augmentation: otp_events.csv/traffic_index.csv/README.md
# ARE committed (the attribution story runs on OTP + traffic + the real
# delay_reason). eta_log.csv is the least load-bearing of the three and
# roughly doubles the folder's weight -- regenerate it on demand with
# scripts/make_synthetic.py's own command (same --seed, byte-identical
# output) rather than carrying it in the repo/every Render build.
data/synthetic/eta_log.csv
